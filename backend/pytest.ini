[pytest]
testpaths = tests
# The tests import the application as `app`, which lives beside this file
# rather than in the directory pytest happens to be started from.
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
